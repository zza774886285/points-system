services:
  points:
    build: .
    container_name: points-system
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/app/data
    env_file: .env
    environment:
      - TZ=Asia/Shanghai
